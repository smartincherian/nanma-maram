# Exposes the package.json version to the React app at build time.
# npm sets npm_package_version when running scripts; CRA (dotenv-expand)
# resolves the reference below.
REACT_APP_VERSION=$npm_package_version
